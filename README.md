# IAB Message Queue Tester

### How to use this:

* Clone this repository.
* Install the dependencies: `cd app-template` & `npm install`
* Start the live-server and launch the application: `npm start`
